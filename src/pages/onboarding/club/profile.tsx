import { OnboardingLayout, ProfileInfo } from '@/components';

function Profile() {
    return (
        <OnboardingLayout>
            <h1>Club Name and Logo</h1>
            <ProfileInfo />
        </OnboardingLayout>
    );
}

export default Profile;
