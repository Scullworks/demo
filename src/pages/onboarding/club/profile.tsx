import { OnboardingLayout, ProfileInfo } from '@/components';

function Profile() {
    return (
        <OnboardingLayout>
            <h1>Your club&apos;s name and logo image</h1>
            <ProfileInfo />
        </OnboardingLayout>
    );
}

export default Profile;
